---
description: Start to work with git
---

# Daily Start Workflow

Routine to begin the day's work.

1. **Morning Alignment**
   - Use `sequential-thinking` to define the primary goal.

2. **Sync Codebase**
   - // turbo
   - `git pull`
   - `git status`

3. **Review Task List**
   - Check `task.md`.
   - Select next task.

4. **Planning**
   - Create/Update `implementation_plan.md` if starting new feature.